import React from "react";
import Character from "./Character";

const Characters = (props) => {
  const { characters } = props;

  return <div>rendered from characters.js</div>;
};

export default Characters;
